spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.bvjphekkgeoclhyguikl
    password: m3Slh7oAt1BiFQIf
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  cloud:
    compatibility-verifier:
      enabled: false

server:
  port: 8081

# Blockchain Configuration
blockchain:
  rpc:
    url: http://localhost:8545
  asset-token:
    contract-address: "0xAssetTokenAddress"
  health-token:
    contract-address: "0xHealthTokenAddress"
  hospital-financials:
    contract-address: "0xHospitalFinancialsAddress"

# Service Configuration
service:
  token:
    conversion-rate: 100.0
  benefits:
    profit-share-percentage: 20.0

# Logging configuration
logging:
  level:
    com.fixed_asset.patient_service: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.jdbc.core.JdbcTemplate: DEBUG

# Eureka Discovery Client (if using service discovery)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    enabled: false
  instance:
    prefer-ip-address: true